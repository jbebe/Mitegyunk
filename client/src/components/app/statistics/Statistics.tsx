import React from 'react';

function Statistics() {

    const title = 'Statisztika';

    return (
        <div className={'card'}>
            <header>{title}</header>
        </div>
    );
}

export default Statistics;
