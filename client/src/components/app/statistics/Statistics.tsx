import React from 'react';

function Statistics() {

    const title = 'Statisztika';

    return (
        <div className={'card card-wide'}>
            <header>{title}</header>
        </div>
    );
}

export default Statistics;
